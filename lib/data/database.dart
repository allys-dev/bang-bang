import "package:supabase_flutter/supabase_flutter.dart";

class Database {
  final supabase = Supabase.instance.client;

  //Read
  // final stream = Supabase.instance.client.from('game_rooms').stream(primaryKey: ['game_code'],
  // ).map((event) => ,);
}


